services:
  connector_nginx:
    image: nginx:latest
    container_name: connector_nginx
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"
    networks:
      - connector
networks:
  connector:
    external: true